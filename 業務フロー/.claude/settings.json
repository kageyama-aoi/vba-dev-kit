{
  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": [
      "Bash(git *)",
      "Bash(gh *)",
      "Bash(pwsh *)",
      "Bash(powershell *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json;from datetime import datetime;d=json.load(sys.stdin);cmd=d.get('tool_input',{}).get('command','');open('C:/Users/kageyama/Tools/マクロ/.claude/bash-log.txt','a',encoding='utf-8').write('['+datetime.now().strftime('%Y-%m-%d %H:%M:%S')+'] '+cmd+'\\n')\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
